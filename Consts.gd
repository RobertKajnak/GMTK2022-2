extends Node

const prompts = [\
	['Hello employee, you need to generate a random outcome that has 25% chance for option A and 75% chance for option b.\n'+\
	'You have access to any number of standard coins (d2)',
	2]
]
# Declare member variables here. Examples:
# var a = 2
# var b = "text"


# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
